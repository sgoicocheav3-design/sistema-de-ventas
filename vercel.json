{
  "version": 2,
  "framework": null,
  "installCommand": "npm install --prefix src/backend && npm install --prefix src/frontend",
  "buildCommand": "npm run db:generate --prefix src/backend && npm run build --prefix src/frontend",
  "outputDirectory": "src/frontend/dist",
  "functions": {
    "src/api/index.js": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/src/api" }
  ]
}
