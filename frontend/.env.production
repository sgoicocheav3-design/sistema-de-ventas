# En producción (Vercel), el frontend y la API están en el mismo dominio.
# NO definas VITE_API_URL en Vercel → el código usará '/api' como fallback,
# lo que funciona perfectamente gracias al rewrite en vercel.json.
# VITE_API_URL=  ← dejar vacío o no definir en Vercel
