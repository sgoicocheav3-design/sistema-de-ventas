{
  "name": "minimarket-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n backend,frontend -c blue,green \"npm run dev --prefix src/backend\" \"npm run dev --prefix src/frontend\""
  }
}
